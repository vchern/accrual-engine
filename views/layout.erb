<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Lambda Accrual Engine</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif; }
    .num { font-variant-numeric: tabular-nums; }
  </style>
</head>
<body class="bg-slate-50 text-slate-900 min-h-screen">
  <header class="border-b border-slate-200 bg-white">
    <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
      <a href="/closes" class="font-semibold tracking-tight text-slate-900">
        Lambda <span class="text-slate-400">/</span> Accrual Engine
      </a>
      <nav class="text-sm text-slate-600 space-x-4">
        <a href="/closes" class="hover:text-slate-900">Closes</a>
        <a href="/import" class="hover:text-slate-900">Data import</a>
      </nav>
    </div>
  </header>
  <main class="max-w-7xl mx-auto px-6 py-8">
    <%= yield %>
  </main>
</body>
</html>
